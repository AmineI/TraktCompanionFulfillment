{
  "name": "trakt-companion_fulfillment",
  "version": "0.0.0",
  "description": "Todo | Hosted on firebase",
  "license": "todoo, especially if using node library for trakt",
  "author": "Amine",
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase serve --only functions --host 0.0.0.0",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "deploy-beta": "firebase deploy --only functions -P Beta",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "actions-on-google": "^2.12.0",
    "firebase-admin": "^8.10.0",
    "firebase-functions": "^3.6.0",
    "request": "^2.88.2",
    "request-promise": "^4.2.5"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-plugin-promise": "^4.2.1",
    "firebase-functions-test": "^0.2.0"
  },
  "private": true
}
